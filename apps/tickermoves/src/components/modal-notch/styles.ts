import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  component: {
    marginTop: 15,
    marginBottom: 25,
    height: 5,
    width: 60,
    borderRadius: 20,
    marginLeft: 'auto',
    marginRight: 'auto',
  },
});

export default styles;
